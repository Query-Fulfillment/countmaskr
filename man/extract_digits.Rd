% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_digits.R
\name{extract_digits}
\alias{extract_digits}
\title{Extract Numeric Values from Formatted Strings}
\usage{
extract_digits(values)
}
\arguments{
\item{values}{A character vector of numeric-like values.
Values may contain optional \code{<} or \code{>} symbols at the start, commas as thousand
separators, and decimal points.}
}
\value{
A numeric vector containing the extracted numeric values. If a value is
incorrectly formatted, the function stops with an error message.
}
\description{
This function extracts numeric values from character strings that may contain optional
symbols (\code{<} or \code{>}), commas, or decimal points. It removes these symbols and formats
the strings to ensure proper numeric conversion. The function checks for disallowed
formats and throws an error if any values do not meet the specified pattern.
}
\details{
The function first checks for valid formats using a regular expression. Only values
matching the following pattern are accepted:
\itemize{
\item Optional \code{<} or \code{>} symbol at the start.
\item Optional negative sign (\code{-}) before the number.
\item Digits in groups of three with commas as thousand separators.
\item Optional decimal point with digits following.
}

If the format is valid, the function removes the \code{<} or \code{>} symbols and commas,
and converts the cleaned string to a numeric value.
}
